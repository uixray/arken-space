# Cloud handoff — Arken Space — 2026-08-21

Документ для продолжения работы в облачной сессии Claude. Он описывает, что в
удалённом окружении делать **можно**, а что физически нельзя — и почему.

## Состояние

- Ветка `main`. Точный tip брать из `git rev-parse HEAD`, а не из этого файла.
- Untracked `apps/web/src/assets/` и `docs/stickers/` исключены намеренно и в
  репозиторий не попадают.
- Если облачная сессия видит `origin/main` на `42c7ccc` (17.08.2026), значит
  локальная серия ещё не запушена и вся работа за 20-21.08 в этом клоне
  отсутствует. Не начинать в таком состоянии: запросить push у владельца.

Актуальный контекст: [current-state.md](./current-state.md),
[uix-517-uix-518-checkpoint-2026-08-21.md](./uix-517-uix-518-checkpoint-2026-08-21.md),
[uix-408-uix-409-uix-450-checkpoint-2026-08-21.md](./uix-408-uix-409-uix-450-checkpoint-2026-08-21.md).

## Ограничения облачного окружения

| Возможность                     | Статус                                                          |
| ------------------------------- | --------------------------------------------------------------- |
| Vitest, typecheck, lint, build  | доступно                                                        |
| Работа с кодом                  | доступно                                                        |
| Docker multiplayer gate         | **недоступно** — нужен Docker-in-Docker                         |
| Измерения на боевом дампе       | **запрещено** — данные не покидают локальную машину             |
| Browser QA (Chromium + Firefox) | требует PostgreSQL и dev-стенда; проверять доступность на месте |

**Первое действие облачной сессии — инвентаризация окружения**, а не план:
`docker version`, наличие PostgreSQL, установлены ли браузеры Playwright,
работает ли Linear MCP. Отчитаться фактами. Не выдавать недостижимый gate за
пройденный.

## Что работает без `.env`

`.env` в репозиторий не входит (`.gitignore`), и в облачном клоне его не будет.
Это проверено, а не предположено:

- `pnpm install --frozen-lockfile`, `pnpm build`, `pnpm typecheck`, `pnpm lint`,
  `pnpm format:check` и `pnpm test` работают **без** `.env` и без внешнего
  PostgreSQL. Тесты поднимают PGlite в процессе, а не подключаются к базе.
  Полный прогон — 171 файл / 1251 тест.
- `.env` нужен только двум вещам: dev-серверу (`apps/server/src/env.ts`
  требует `DATABASE_URL` и остальные переменные) и e2e, где без
  `GM_ACCESS_TOKEN` часть тестов скипается.
- Проект **не использует dotenv** и не передаёт `--env-file`. Переменные надо
  экспортировать в окружение самому: `set -a; . ./.env; set +a`. Шаблон —
  `.env.example`.

Иными словами, обязательный gate (`build`/`typecheck`/`lint`/`test`) в облаке
достижим полностью. Ограничены только dev-стенд, e2e и multiplayer.

## Чего в репозитории нет

`AGENTS.md`, `.claude/` и `docker-compose.override.yml` перечислены в
`.gitignore` и в облачный клон не попадают. Не искать их и не считать
отсутствие ошибкой; более старый `docs/claude-handoff-2026-08-21.md` ссылается
на `AGENTS.md` — в облаке эта ссылка нерабочая.

`docker-compose.override.yml` пробрасывает PostgreSQL на `127.0.0.1:5433`
только на локальной машине владельца, где порт 5432 занят нативной службой. В
облаке действует обычный `docker-compose.yml`.

## Что нельзя делать

- Не запускать `scripts/measure-broadcast.ts`. Измерения UIX-408/409/450
  требуют изолированной копии production-дампа и выполняются только локально.
- Не считать multiplayer gate пройденным без Docker.
- Не push и не deploy в production без явного запроса.
- Не трогать `apps/web/src/assets/` и `docs/stickers/`.
- Не использовать `git add -A`.

## Ловушка при проверке

`pnpm test:multiplayer` и e2e-обёртки маскируют настоящий код возврата, если
вывод уходит в пайп: процесс завершается нулём, тогда как прогон красный.
Проверять `${PIPESTATUS[0]}`, а не код процесса.

## Что уже сделано (21.08.2026)

Тройка UIX-408 / UIX-409 / UIX-450 реализована и прошла все локальные gates,
кроме измерений:

- полный Vitest — 171 файл / 1251 тест PASS;
- typecheck, lint, production build — PASS;
- изолированный Docker multiplayer — 2 passed, включая `backend-restart`;
- целевой browser QA Chromium + Firefox — 22 passed.

Попутно закрыт UIX-517 (поповеры MusicBar перекрывали вкладки чата) и заведён
UIX-518 (нестабильность полного browser gate).

## Открытые блокеры

1. **Измерения UIX-408/409/450** — единственное, что мешает закрыть три Urgent
   задачи. В облаке неустранимо: нужна изолированная копия дампа
   `arken-20260815T065736Z`, восстановленная в loopback-контейнер, запуск с
   `ARKEN_MEASURE_CONFIRM=isolated-copy`, явными campaign/viewed-scene UUID и
   ровно 1 GM + 6 PLAYER. Делать только локально.
2. **UIX-518** — e2e делят одну кампанию и одну базу.
3. **Тихий fallback в `packages/db/src/migrate.ts`** — при отсутствии
   `DATABASE_URL` миграции молча уходят на `localhost:5432`. Способен применить
   миграции к посторонней базе.
4. **UIX-519** — корневые Playwright-конфиги вне типизации. Именно эта дыра
   пропустила нерабочий `actionTimeout` и на несколько прогонов замаскировала
   настоящую причину падения multiplayer gate.

## Рекомендуемый пул для облака

Порядок — связанными пулами, с полным gate на итоговом содержимом:

1. **UIX-519** — покрыть типизацией корневые Playwright-конфиги. Полностью
   выполнимо и проверяемо в облаке: критерий — заведомо неверное свойство в
   конфиге роняет `pnpm typecheck`, проверенное намеренной поломкой. Хорошая
   первая задача, чтобы убедиться в работоспособности окружения.
2. **Fallback в `migrate.ts`** — убрать, падать с явной ошибкой, проверить
   остальные точки входа (`grep` по `localhost:5432` и `?? "postgres://`),
   задокументировать передачу `.env` в `docs/development-guide.md`.
3. **UIX-518** — независимое состояние для каждого e2e (отдельная кампания на
   файл, либо сброс и повторный seed между файлами). Критерий: два полных
   прогона подряд на неизменном дереве дают одинаковый результат, порядок
   файлов на исход не влияет. Требует работающего e2e-стенда — браться после
   инвентаризации, а не вслепую.
4. **Реконсиляция In Review** — UIX-467, UIX-491, UIX-462, UIX-465: сверять
   фактический код с acceptance, не переимплементируя вслепую.
5. **Аудит UIX-431 и UIX-407.** UIX-407 — измерительный gate для дальнейшей
   декомпозиции `App.tsx`; UIX-398 не продолжать без его измерений.

UIX-382, UIX-245 и UIX-364 держать отдельными продуктовыми пулами.

## Отчётность

Linear — источник задач, статусов и acceptance; Git — источник реализации.
Обновлять Linear только на stage gate: ревизии, точные команды, результаты,
блокеры. Done — только после всех acceptance criteria. Что проверить нельзя —
писать прямо, без подмены оценкой.
