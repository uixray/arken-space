# Production release — 18 сентября 2026

## Результат

На `https://arken-khar.space` работает **cce56397a6b1e91fcf2fc6951e506d64b62647cb**.
Технические gates и postflight завершены. Это не ручная приёмка GM+6 и не
закрытие всех задач интерфейса. Новая документация остаётся в отдельной локальной
ветке и не была добавлена в замороженный runtime-кандидат.

## До выкладки

- Exact-main checks [35388600458](https://github.com/uixray/arken-space/actions/runs/35388600458) SUCCESS.
- E2E [35388600465](https://github.com/uixray/arken-space/actions/runs/35388600465): четыре shards + aggregate SUCCESS; 977 PASS, 29 skipped, 0 failed/flaky/errors. Skips не считаются приёмкой.
- Multiplayer [35388600599](https://github.com/uixray/arken-space/actions/runs/35388600599): 3/3 PASS, exact revision; GM+6 synthetic, shared-browser identity recovery, image/real OGG upload, playback, reload/restart, cleanup без остатка.
- Unconfirmed backup `51aeeb2508d6fb91dccb6e7c18b0df06a590429036a6dc07f6b276e5b726d4fc`: изолированное восстановление 22/22 PASS.
- Confirmed fresh backup **ec6b8217d231fa4700ef52462c8a686391d0a29f65b8e7b4f95d7d5cdca4de48**: восстановление 22/22 PASS; exact target/schema 2, 44 миграции, все 55 таблиц, checksums 120 медиа, cleanup без контейнеров/томов. Старый production во время репетиции оставался здоровым.

## Остановки и восстановление gates

1. Первая confirmed-попытка остановилась до изменения прода: timeout GitHub SSH:22. Проверен SSH:443 с тем же deploy-key и существующим доверенным host key; transport override только на одну команду, постоянные настройки не менялись. Все host gates выполнены заново.
2. После успешного build/start `release.sh` завершился **exit 1 на disk gate**, а не успехом: доступно 6917517312 при обязательных 7516192768 байтах. Порог не снижался.
3. Удалены только 19 заранее перечисленных IDs reclaimable/non-shared build cache Arken. Манифесты всех образов и томов до/после идентичны; rollback images и данные не удалялись.
4. Disk gate повторно PASS: 8515612672 байта; после restart 8515461120. Оставшиеся host gates завершены документированным ручным fallback, без повторного build/deploy уже работающего target. Поэтому raw exit `release.sh` остаётся 1; итоговый успех подтверждён отдельными recovery/postflight receipts.

## После выкладки и контролируемого restart

- Health/database `ok`, exact SHA, schema 2.
- GM login/diagnostics 200; cookie HttpOnly/Secure/SameSite Strict; logout 200 и последующий 401.
- WebSocket upgrade 101 до и после restart.
- Все 55 table counts совпали до deploy, после deploy и после restart. Медиа: 120 файлов, 74699580 байт, неизменный общий SHA-256; `.env` digest/owner/mode 600 сохранены. Игровые данные не сбрасывались; тестовых uploads в live-кампании нет.
- Browser после restart: карта и портреты загружены и визуально проверены, reload PASS, 0 page errors и 0 ошибок авторизованных HTTP-ответов. Реальное существующее audio возвращает 206 с payload. JS/CSS: три реально полученных payload совпадают по SHA-256 с файлами выпущенного web-container.
- Первые вспомогательные browser-наблюдения не приняты: ранний screenshot до медиа, зависший decode lazy images, попытка читать тело 304. Проверенный финальный smoke учитывает readiness и bodyless responses; это исправления локального наблюдателя, не продукта. Зависшее собственное process tree остановлено; финальный browser/context закрыт, logout проверен.

## Точка отката

Предыдущий production: `7f28ca399ec0530e55e6bd41d4427ea23150522b`.

- `arken-space-rollback-server:7f28ca399ec0530e55e6bd41d4427ea23150522b` → `sha256:099431afd486bad9119515ad8191a5ed31f66911516c02e16daf02cfc6df9232`.
- `arken-space-rollback-web:7f28ca399ec0530e55e6bd41d4427ea23150522b` → `sha256:4229380be358337f3322053eae1b4338bce15a193be0ad7c7f94b4351f50cceb`.
- Deployed server `sha256:6e4bb25b1c383a445855c7e9c384526cb977806d967e8bf6d8ae9c519f055624`; web `sha256:a170f2f6eab9846dc987babd7fe0cd760ce3509e99f522b518c3b3a7db347ecc`.

## Ограничения и продолжение

Человеческая сессия GM+6, физические телефоны/Safari, субъективное прослушивание
звука, полная приёмка темы и всего menu registry не заявлены. UIX-502 отдельно
сверена по восьми исходным AC и готова к закрытию; Linear не менялся из-за
предыдущего ограничения на запись. Иные Review/In Progress закрывать только по
их собственным AC. План продолжения — [единая точка входа](README.md).

Локальные receipts оператора:
`C:\Users\UIXRay\.codex\visualizations\2026\09\16\01a0a7d5-b072-7022-8e9d-4538c0a92b07\release-2026-09-18`.
Ключевые файлы: `restore-confirmed-cce5639.json`, `postflight-host-after-restart.log`,
`persistence-comparison.json`, `production-runtime-hashes.json`,
`browser-postflight-verified/production-browser-postflight.json`.
