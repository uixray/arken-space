# UIX-644 — объединённый UI-пул

## Решения и ревизии

- База: `8dadb9ae295560c6f225cce5de5be522683393ab`.
- Локальная ветка: `codex/uix-644-review-pool`.
- Проверенная ревизия кода и тестов: `933d09a4592403075009b169bdd09e3e6d2bcd91`.
- Пул объединяет готовые срезы UIX-644/507, UIX-421, UIX-423 и UIX-414. Это не завершение всех критериев крупных родительских задач.
- UIX-645 и другие рабочие ветки в пул не включены. Данные кампаний, API, серверные права и схема БД не менялись.

## Состав

- **644:** исправление обрезанного выбора сцены, pointer/keyboard lifecycle, ограничение меню доступным viewport, реестр и регрессионная защита overlay-контракта.
- **507 через 644:** постоянные счётчики выделения убраны; групповая кнопка отделена от zoom и находится ниже списка объектов. Количество и типы объектов остались в подтверждении удаления.
- **421 A1:** единицы и диапазоны полей сцены, native color control, связанная валидация и причина недоступности сохранения; нулевая прозрачность сетки допустима.
- **423:** согласованные названия «Редактор мира» и «Справочник мира» без изменения routing IDs и прав.
- **414:** два критичных контракта настоящего Sidebar: GM может перейти в редакторы, PLAYER не получает мастерские переходы.
- Точный список файлов: `git diff --name-only 8dadb9a..933d09a`; отдельные checkpoints описывают владельцев и локальные изменения.

## Проверка общего кандидата

- Полные build, format, lint и typecheck: **PASS**. Существующие предупреждения lint не маскировались и не исправлялись вне этого среза.
- Полный Vitest: **232 файла / 1838 тестов PASS**, exit 0, 497.53 s. Включены новые три real-Gravity DOM-сценария SelectionActions.
- Chromium и Firefox, retries 0: **36 сценариев меню/навигации/форм PASS** и **12 сценариев HUD/selection/ACL PASS**. Это два связанных прогона, а не один вымышленный зелёный job на 48 тестов. После общего прогона менялся только тестовый fixture, не production-код.
- HUD проверяет 0/1/несколько токенов и рисунков, смешанную группу, Escape, пустой клик, плюс/минус, native slider, процент и «Вписать». Выбор подтверждается действиями и payload, а не отсутствием удалённого текста.
- Смешанная группа сохраняется при закрытии списка; реальные спорные пиксели проверены через `elementFromPoint` и pointer click без `force`. GM MOVE/DELETE, PLAYER ACL/отзыв доступа и отмена опасного действия сохранены.
- Просмотрены синтетические изображения desktop/compact, в том числе GM/PLAYER compact в обоих браузерах. Это не реальная production/многопользовательская или аппаратная приёмка.

## Отрицательные проверки и исправленные ошибки проверки

- На старом HUD первый выбранный токен сдвигал zoom x с `843.78125` до `600.9375`: новый browser-тест воспроизводит **FAIL** на разнице `242.84375` CSS px.
- Возврат `z-index:26` у групповой кнопки даёт **FAIL** реального hit-test строки списка; постоянный счётчик в SelectionActions даёт **2 FAIL / 1 PASS** DOM-сценариев. Оба файла восстановлены побайтно в `finally`, SHA-256 совпали; восстановленный browser-case снова **PASS**.
- Первый новый helper ошибочно считал trigger переключателем. Исправлен только тест: закрытие списка через Escape с проверкой hidden/aria-expanded; production Escape не менялся.
- На compact начало тестовой рамки попадало в расширенную hit-area рисунка. Trace доказал неожиданный PATCH, а ошибочный fixture отвечал 204 вместо DrawingDto. Начало жеста отнесено в пустую область; неожиданные мутации теперь отклоняются и явно проверяются. Ожидания состава группы не ослаблялись.
- Один dev-server завершился во время общего запуска, после чего появились connection refused. Это не десятки продуктовых дефектов. Финальные браузерные проверки выполнены на свежем Vite без параллельного build; сервер остановлен до полного quality gate.

## Открытые границы и следующее действие

- Полный UIX-644 inventory, остальные вложенные слои, native/browser zoom и аппаратная матрица не объявляются принятыми по этому срезу.
- UIX-421: preview, остальные формы и глобальные пустые/disabled состояния остаются отдельной работой. UIX-414: широкое компонентное покрытие не закрыто двумя guards.
- Отложенные production PLAYER/hot-seat/GM+6 проверки не превращаются в PASS. Ранее опубликованные задачи не нужно выкладывать повторно ради статуса In Review.
- Далее: общий PR и обязательные GitHub checks/e2e/multiplayer точного кандидата. Production — отдельный завершённый релизный gate с backup/restore/rollback и smoke; этот локальный checkpoint не разрешает обходить его.
- Перевод документации и добавление этого checkpoint после проверки — только документационные изменения; перед отправкой проверяется их формат и неизменность технических значений.

## Подготовка после CI — публикация приостановлена 06.09.2026

- По новому прямому указанию владельца выполняется **только локальная подготовка**. Новые push, merge, обновления публичного PR и deploy запрещены до отдельного разрешения. Ранее открытый PR #69 не обновлялся после этого указания; production не менялся.
- Предыдущий GitHub E2E завершился **327 passed / 4 failed / 1 flaky / 4 skipped**. Успешные checks и isolated multiplayer не заменяют этот gate. Основные ошибки: синхронная проверка modal focus при переходе через guard и ограничение частоты запросов при подготовке журнала.
- Локальная ревизия коррекции тестов: `9864c579bbde5f30409cbc76023c9a9c2817d0f2`. Изменены только `tests/e2e/mobile-foundation.spec.ts`, `tests/e2e/modal-focus.ts`, `tests/e2e/modal-focus.spec.ts`, `tests/e2e/follow-scroll.spec.ts`, `tests/e2e/chat-seed.ts`, `tests/chat-seed.test.ts`. Production-код, API, лимиты и данные не менялись.
- Focus helper сохраняет восемь Tab и строгий итоговый `dialog.contains(activeElement)`. Учитывается асинхронный возврат только с собственного direct-sibling guard UIKit; recorder фиксирует любой уход на фон, даже если фокус позже восстановился. Исходный запрет pointer-клика по фону и сценарий смены игрока сохранены.
- Chat seed соблюдает pacing и валидный Retry-After только для 429: максимум два повтора на весь seed, тот же payload/actionId, ограниченное добавление времени только для cooldown. Количество записей, scroll/poll assertions и production rate limit не изменены. Исходный пользовательский дефект UIX-475 этим не объявляется исправленным.
- Полный локальный build, format, lint и typecheck: **PASS**. Vitest на `9864c57`: **233 файла / 1863 теста PASS**, exit 0, 481.27 s. Старые предупреждения lint/jsdom не выдаются за runtime evidence.
- Локальный synthetic App handoff 360/820 в Chromium и Firefox: **4/4 PASS**, retries 0. Каждый случай проверяет Tab/Shift+Tab, затем намеренное отключение возврата с guard с ожидаемым падением строгого oracle и восстановленный успешный цикл. Это не полный backend mobile journey или production handoff.
- Seed unit: **25/25 PASS**. Диверсии нового actionId, превышения retry budget и повторов HTTP 503 обнаружены; helper восстановлен побайтно перед полным локальным gate.
- Новый общий **E2E с реальным backend ещё не выполнен**, прежний retry timeout отдельно не объявляется устранённым. Удалённый CI нового SHA не запускался, поскольку потребовал бы публикации commits. Следующее действие после отдельного GO — обновить кандидат и пройти полный CI; до этого пул подготовлен локально, но не имеет завершённого релизного gate.

## Локальный пул подготовки — 07.09.2026

### Решения и граница

- Продолжение от `9425dc67f3b2a47f1099bc5bd7eb96bc063b7f82`; публикация по-прежнему приостановлена. Ни push, ни обновления PR, ни merge/deploy в этом пуле не выполняются.
- Сверены все 33 карточки Review и 20 In Progress. Новые Backlog задачи не брались: уже опубликованным реализациям нужна адресная приёмка, а не повторный перенос. Полные родительские задачи не закрываются по готовому дочернему срезу.
- Текущий связанный срез — UIX-644 и UIX-421 A2. Сервер, API, ACL, схема, данные кампаний и чужие worktree не меняются.

### Изменённые файлы и поведение

- `apps/web/src/MapToolbar.tsx`, `apps/web/src/styles.css`: подпись/значение радиуса и ползунок размещены внутри панели, включая свёрнутое состояние; прежние значения, диапазон и callback сохранены. До исправления реальная правая граница ползунка была 295.734375 px при границе панели 259.171875 px.
- `apps/web/src/ui/SceneManagerDialog.tsx`, новые `SceneGridPreview.tsx`/`SceneGridPreview.css`: read-only предпросмотр карты и сетки из текущего draft формы. Он не подменяет сетку основного Canvas и не меняет привязку/измерения до Save. Отмена, Escape и закрытие отбрасывают draft; ошибка сохраняет его для повторной попытки.
- Невалидные числовые поля заменяют SVG понятной причиной. Прозрачность 0 остаётся допустимой. Пока выбрана новая ещё не сохранённая карта, старый SVG не выдаётся за её предпросмотр: показывается явное ограничение. Существующий intake thumbnail/upload pipeline не менялся.
- `apps/web/src/use-mutation-runners.ts`, `apps/web/src/use-scene-actions.ts`: только create/PATCH формы сцены отдают ошибку действия вызывающей форме. Inline-ошибка имеет `role="alert"`; дублирующий глобальный toast больше не должен закрывать повторный Save. Безусловный rethrow, успешный reload и глобальное сообщение об ошибке reload сохранены. Остальные действия используют прежний глобальный канал. Общие CSS/z-index toast не менялись.
- `SceneManagerDialog.dom.test.tsx`, `use-mutation-runners.test.tsx`, `use-scene-actions.test.tsx`: реальные компонентные и hook-контракты. `tests/e2e/scene-editor-controls.spec.ts`: draft/close/reopen, pending upload и pointer retry. `tests/e2e/canvas-token-regressions.spec.ts`: Canvas pixel oracle кисти, пределы радиуса, реальные состояния/zoom и отсутствие случайных мутаций при закрытии списка. `tests/e2e/concept.spec.ts`: адресные изображения иерархии безопасных/опасных действий до их выполнения.

### Проверки и найденные ограничения

- Scoped hooks + настоящий SceneEditor: 3 файла / 23 теста PASS. Production-диверсия привязки preview к initial вместо draft обнаружена неизменным DOM oracle; исходные байты восстановлены.
- UIX-611: неизменный компонентный тест обнаружил намеренный production payload с исходным IMAGE вместо derivative TOKEN; baseline и восстановленный набор — 10/10 PASS, диверсия — 1 адресный FAIL / 9 PASS. Это не реальная генерация/сервер; source-изменений от проверки не осталось.
- Браузерный проход выявил настоящий выход ползунка и перекрытие Retry Save самой карточкой toast. Проверки не обходились через force-click, скрытие toast, ослабление focus или подмену ожидаемых значений.
- Старый fixture состояний ошибочно считал trigger списка переключателем. Теперь закрытие явно проверяется по Escape/aria-expanded; PNG открытого списка не считается доказательством дефекта inline editor. После закрытия zoom получает реальные pointer clicks.
- Результат общего браузерного и полного quality gate этого среза фиксируется ниже после завершения; прежний публичный красный E2E не объявляется исправленным одним локальным прогоном.

### Остаток и следующее действие

- A2 ограничен read-only preview сохранённой/выбранной готовой карты; bytes pending upload не накладываются на сетку до сохранения. Остальные формы и общие empty/disabled состояния UIX-421 остаются отдельной текущей работой.
- Полный UIX-644 inventory, одновременные popup разных владельцев/вложенные modal, native zoom и аппаратная матрица не закрыты этим срезом.
- Production PLAYER/hot-seat, живой многопользовательский и аппаратный остаток не превращаются в PASS. Full real-backend E2E точного кандидата и отдельный GO всё ещё обязательны перед публикацией.
- Далее — зафиксировать общий локальный gate, затем продолжать существующие In Progress по их зависимостям. Новый Backlog пул пока не брать.

### Браузерный gate 07.09

- 14 проверок форм/иерархии действий PASS в общем прогоне; 8 проверок Canvas PASS в восстановленном прогоне обоих браузеров, retries 0. Это 22 уникальных адресных сценария, а не вымышленный единый зелёный job. Production-код между этими прогонами не менялся.
- В первом прогоне новый helper для вертикального range использовал ArrowRight: Chromium менял значение, Firefox оставлял 0.25. Исправлена только тестовая клавиша на вертикальную ArrowUp; прежнее требование zoom=1 и все pointer/pixel assertions сохранены. Восстановленный Canvas-пул — 8/8 PASS, 55.3 s.
- Проверены/просмотрены синтетические изображения 1280/390 в обоих браузерах: four-condition labels/badges, контур кисти, contained radius, безопасное открытие сцены против публикации, subordinate delete, draft preview. Это не production, устройство или multiplayer receipt.
- Предыдущий долгоживущий Vite завершился посреди первой попытки; последующие connection refused не классифицированы как продуктовые дефекты. Финальные адресные проверки выполнены на свежем Vite. Собственный Vite остановлен до полного quality-gate.

### Полный quality-gate и передача

- Production build PASS; production-файлы после сборки и браузерной проверки подтверждены неизменными по SHA-256. Полные format, lint и typecheck PASS. Остались 4 прежних lint warnings; они не исправлялись вне scope.
- Полный Vitest: **233 файла / 1875 тестов PASS**, exit 0, 494.68 s. Вывод mock health-contract и ограничения jsdom media не выдаются за новый production/runtime receipt.
- Первый typecheck выявил только ошибки типов новых DOM-тестов: неподдерживаемый Testing Library `exact` и не типизированные пары значений. Исправлены только test options/tuple types, без изменения literal names, входов и ожиданий; повторный полный typecheck и Vitest PASS.
- Локальный проверенный срез готов к следующему gate, но **не имеет нового full real-backend E2E/публичного CI**. Ревизия фиксируется коммитом этого checkpoint; production source после проверок не менялся.
- Далее подготовлена совместная приёмка существующих UIX-448, UIX-584/511 и UIX-589. Существующий real multiplayer уже содержит pause/restart/resync; не создавать harness заново. Требуются доступный локальный backend, свежий receipt кандидата и недостающие UI/compact/Firefox шаги. Новые Backlog задачи не брать до разбора остатка In Progress.
- Linear и публикационные статусы этим локальным пулом не менялись. Закрытие полных родительских задач требует отдельной адресной приёмки.
