# UIX-619 — Интеграция проверенных веток Antigravity

Владелец разрешил завершить исправления, пройти гейты, затем merge и production.

## Объём и порядок

1. Объединить 9c11efe (включая предшествующий asset lifecycle), e857db7
   (полный пакет страниц) и cbaa390 в собственной ветке от origin/main d789d97.
2. Исправить измеренные 40px targets, потерю DOM-фокуса при тиках диспетчера,
   невоспроизводимое форматирование players:build и атрибуцию декомпозиции.
3. Добавить регрессии и проверить их диверсией production-кода.
4. Один общий quality gate, полный E2E и Docker multiplayer на итоговом коде.
5. PR, зелёный GitHub CI, merge; затем host backup/restore и release по runbook.

Исходные ветки Antigravity не меняются. Незавершённые UIX-448 и прочие отдельные
worktree в релиз не входят. Декомпозиция относится к UIX-398; UIX-407 с замерами
производительности не закрывается этим рефакторингом.

## Начальное состояние

Docker 29.7.2 доступен с правами вне песочницы, перезапуск не потребовался.
Три ветки слиты локально без конфликтов. Гейты и публикация пока не выполнены.

## Checkpoint проверки 2026-09-03

- Ревизии: cddbf28 — исправления ревью; 980191c — перенос encoding-проверки
  на выделенный MapToolbar. PR #56, исходные ветки сохранены.
- Изменены: размеры контролов токена, DOM-обновление диспетчерской, генератор
  страниц и его результаты, scoped browser globals ESLint, regression-тесты,
  атрибуция документа декомпозиции.
- Локально format/lint/typecheck/build PASS. Полный Vitest дал 1733 PASS и один
  устаревший путь теста; после его исправления GitHub checks PASS (7м39с).
- Narrow Chromium/Firefox: 2/2 PASS. Реальный DOM-тест диспетчера и
  воспроизводимость генерации: PASS. Диверсии production-кода подтвердили
  потерю фокуса, неформатированную генерацию, 40px target и испорченную подпись.
- Локальный isolated Docker multiplayer на 980191c: 2/2 PASS, restart,
  cleanup и resource-leak-check PASS; production не опрашивался. GitHub
  multiplayer также PASS (4м55с).
- Полный локальный E2E заблокирован аварией Windows Vite с exit 3221226505:
  два запуска теряли frontend, API и БД оставались доступны. Этот результат
  не считается PASS; повторять одинаковый аварийный запуск бессмысленно.
- GitHub E2E на 980191c: 232 PASS, 4 skipped, 2 FAIL — один устаревший текст
  пустой галереи в Chromium и Firefox после UIX-373. Ожидание актуализировано,
  добавлено отсутствие grid после detach; focused оба браузера 2/2 PASS.
- Non-live media smoke на 980191c: PNG преобразован в WebP, MP3 загружен и
  прочитан с duration 0.34 сек. До релиза проверка повторяется на точном target,
  одноразовый контур затем удаляется.
- Production read-only: d789d97, health и nginx PASS, свободно около 8 GiB,
  резерв приложения 2 GiB; сертификат покрывает основной и все девять
  поддоменов страниц. Никаких production-изменений пока нет.
- Следующий шаг: зелёный CI итогового follow-up, merge по разрешению владельца,
  затем точный non-live smoke, backup/restore и host release. Ручную игровую
  приёмку не объявлять выполненной по автоматическим тестам.
